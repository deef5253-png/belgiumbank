version: '3.8'

services:
  belgiumbank:
    build: .
    container_name: belgiumbank
    ports:
      - "5000:5000"
    volumes:
      - ./backend/uploads:/app/backend/uploads
      - ./backend/belgium_bank.db:/app/backend/belgium_bank.db
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=votre_cle_secrete_tres_longue_et_aleatoire_ici
      - JWT_SECRET_KEY=votre_jwt_cle_secrete_ici
      - ADMIN_EMAIL=admin@belgiumbank.be
      - ADMIN_PASSWORD=AdminSecure2024!
      - ADMIN_SECURITY_KEY=BELGIUM-BANK-ADMIN-2024
    restart: unless-stopped
